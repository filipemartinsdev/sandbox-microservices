server:
  port: ${PORT:8080}

spring:
  datasource:
      url: jdbc:h2:mem:test
      username: test
      password: test


  jpa:
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect